<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.covid_examiner">


    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.SEND_SMS" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Covid_Examiner">
        <activity android:name=".statistika"></activity>
        <activity android:name=".diagrafi_upallilou" />
        <activity android:name=".make_upallilos" />
        <activity android:name=".asthenis" />
        <activity android:name=".enimerosi" />
        <activity android:name=".profile_ypallilos" />
        <activity android:name=".profile_Admin" />
        <activity android:name=".ChangeCode" />
        <activity android:name=".Profile" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>